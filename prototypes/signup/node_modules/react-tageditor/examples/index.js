import React from 'react'
import { render } from 'react-dom'
import TagEditor from '../lib/TagEditor'

render(<TagEditor tags={['hello', 'world']} delimiters={[',']} placeholder="Input tags, split by comma" />, document.querySelector('#app'))
